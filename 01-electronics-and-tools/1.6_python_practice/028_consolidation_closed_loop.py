# Consolidation — Closed Loop
#
# This file is a placeholder. Write it after 025–027.
# It should pull together the Phase 5 files: differential-drive motion,
# odometry, aiming at a point, clamping a command, and zeroing that
# command when the last good input is too old or not a real number.
#
# Include all of this:
# - a wheeled robot on a plane (the motion functions, not a separate
#   simulator window)
# - odometry and a range reading
# - commands that stay inside limits
# - a timeout that zeros those commands
# - tests on the geometry and the clamps
# - a record of the run written to a file (a short JSON log is enough
#   here; there is no bag file yet)
#
# Run this file with python once the exercise is written.

print("Placeholder: Closed Loop. See the comments at the top of this file.")
